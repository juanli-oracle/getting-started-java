# docker box definition
box:
  id: openjdk
  ports:
    - "8080"

# defining the dev pipeline
dev:
  steps:
    - script:
      name: gradle bootRun
      code: |
        ./gradlew bootRun

# Build definition
build:
  # The steps that will be executed on build
  steps:
    # A step that executes `gradle build` command
    - script:
        name: gradle build
        code: |
          rm -rf $WERCKER_CACHE_DIR/*
          echo "copy required library files to parse dependency-check report and generate occurrences"
          cp src/main/resources/* $WERCKER_CACHE_DIR
          ./gradlew --full-stacktrace -q --project-cache-dir=$WERCKER_CACHE_DIR build
          echo "cp build/libs/*.jar $WERCKER_CACHE_DIR"
          cp build/libs/*.jar $WERCKER_CACHE_DIR
          echo "cp build/libs/*.jar $WERCKER_OUTPUT_DIR"
          cp build/libs/*.jar $WERCKER_OUTPUT_DIR
    - script:
        name: environment details
        code: |
          echo "% env"
          env
          echo "% ls -alF $WERCKER_CACHE_DIR"
          ls -alF $WERCKER_CACHE_DIR
          echo "% ls -alF $WERCKER_OUTPUT_DIR"
          ls -alF $WERCKER_OUTPUT_DIR

# security scan definition
security-scan:
  # The steps that will run the security scan to check for libraries with reported CVEs
  steps:
    - script:
        name: environment details
        code: |
          echo "% env"
          env
          export PATH=$PATH:$WERCKER_CACHE_DIR
          CLAIR_OUTPUT=High CLAIR_ADDR=http://129.146.68.168:30061 CLAIR_TIMEOUT=10 DOCKER_TIMEOUT=10 DOCKER_USER=juanlioracle DOCKER_PASSWORD=dockerjuanli1 DOCKER_INSECURE=true REGISTRY_INSECURE=true JSON_OUTPUT=true klar-2.0.2-linux-amd64 quay.io/coreos/clair
